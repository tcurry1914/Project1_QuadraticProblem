#We should be able to call make at any level of the hierarchy I think.
#We can put a makefile here which builds to build/. and includes source
#from src/.  In the src directory it would reference ../build/., and vice
#versa.
